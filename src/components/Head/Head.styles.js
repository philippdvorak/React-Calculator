import styled from "styled-components";

const color = "#eee";

export const Number = styled.div`
  font-size: 70px;
  text-align: right;
  margin: 50px 25px 0;
  color: ${color};
  border-bottom: 5px solid ${color};
`;